---
name: mongodb3 
templates:
  ctl.erb: bin/ctl

packages:
- simple_server

properties:
  mongodb.port:
    description: "Port on which server is listening"
    default: 27017 
  mongodb.auth.enable:
    description: "Whether MongDB auth is enabled or not"
    default: false
